import actions from './actions';

const entities = {
    '#0000ff' : {
        name: 'sofa',
        actions: {
            LOOK : {
                text: 'A sofa. Looks cozy, but Matt is sitting on it',
                timeout: 3000,
            }
        }
    }
}

export default entities;
