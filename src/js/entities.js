import actions from './actions';

const entities = {
    '#0000ff' : {
        name: 'sofa',
        actions: {
            LOOK : 'A sofa. Looks cozy, but Matt is sitting on it',
        }
    }
}

export default entities;
